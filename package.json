{
  "name": "learning-chart",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "parcel build src/index.html --public-url ./ && git add dist && git commit -m 'Deploy to GitHub Pages' && git push origin `git rev-parse --abbrev-ref HEAD`"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cubejs-client/core": "^0.35.23",
    "chart.js": "^4.4.3"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
